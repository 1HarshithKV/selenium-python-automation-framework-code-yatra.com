# """Debug, Info,Warning(default logger), error,Critical"""
# import logging
#
# logging.basicConfig(level=logging.INFO)
# logging.critical("This is a warning")

# s=['allen','blake','miller','smith','turner']
# l=[]
# for char in s:
#     if len(char)%2 == 0:
#         l.append(char[::-1])
#     else:
#         len(char)
# print(l)
#
# a=[char[::-1] if len(chr)%2 == 0 else len(chr) for char in s]
# print(a)
